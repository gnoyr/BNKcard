<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bnk.domain.application.mapper.CardApplicationMapper">

    <resultMap id="appMap" type="com.bnk.domain.application.model.CardApplication">
        <id     property="applicationId"      column="application_id"/>
        <result property="userId"             column="user_id"/>
        <result property="cardId"             column="card_id"/>
        <result property="applicationStatus"  column="application_status"/>
        <result property="applyChannel"       column="apply_channel"/>
        <result property="requestedLimit"     column="requested_limit"/>
        <result property="approvedLimit"      column="approved_limit"/>
        <result property="rejectionReason"    column="rejection_reason"/>
        <result property="applicationComment" column="application_comment"/>
        <result property="appliedAt"          column="applied_at"/>
        <result property="reviewedAt"         column="reviewed_at"/>
        <result property="reviewedBy"         column="reviewed_by"/>
        <result property="createdAt"          column="created_at"/>
        <result property="updatedAt"          column="updated_at"/>
        <result property="cardName"           column="card_name"/>
    </resultMap>

    <insert id="insertApplication" useGeneratedKeys="true" keyProperty="applicationId">
        INSERT INTO CARD_APPLICATIONS (
            user_id, card_id, application_status, apply_channel,
            requested_limit, application_comment,
            applied_at, created_at, updated_at
        ) VALUES (
            #{userId}, #{cardId},
            'REQUESTED',
            #{applyChannel},
            #{requestedLimit,     jdbcType=NUMERIC},
            #{applicationComment, jdbcType=VARCHAR},
            SYSTIMESTAMP, SYSTIMESTAMP, SYSTIMESTAMP
        )
    </insert>

    <select id="findById" resultMap="appMap">
        SELECT ca.*, c.card_name
        FROM   CARD_APPLICATIONS ca
        INNER JOIN CARDS c ON ca.card_id = c.card_id
        WHERE  ca.application_id = #{applicationId}
    </select>

    <select id="findByUserId" resultMap="appMap">
        SELECT ca.*, c.card_name
        FROM   CARD_APPLICATIONS ca
        INNER JOIN CARDS c ON ca.card_id = c.card_id
        WHERE  ca.user_id = #{userId}
        ORDER  BY ca.applied_at DESC
    </select>

    <select id="findAdminList" resultMap="applicationMap">
	    SELECT *
	    FROM   CARD_APPLICATIONS
	    WHERE  1 = 1
	    <if test="userId != null">
	        AND user_id = #{userId}
	    </if>
	    <if test="cardId != null">
	        AND card_id = #{cardId}
	    </if>
	    <if test="applicationStatus != null and applicationStatus != ''">
	        AND application_status = #{applicationStatus}
	    </if>
	    <!-- 페이지네이션 필수 추가 -->
	    ORDER  BY applied_at DESC
	    OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
	</select>

	<select id="countAdminList" resultType="long">
	    SELECT COUNT(1)
	    FROM   CARD_APPLICATIONS
	    WHERE  1 = 1
	    <if test="userId != null">
	        AND user_id = #{userId}
	    </if>
	    <if test="cardId != null">
	        AND card_id = #{cardId}
	    </if>
	    <if test="applicationStatus != null and applicationStatus != ''">
	        AND application_status = #{applicationStatus}
	    </if>
	</select>

    <update id="updateStatus">
        UPDATE CARD_APPLICATIONS
        <set>
            application_status = #{applicationStatus},
            updated_at         = SYSTIMESTAMP,
            reviewed_at        = SYSTIMESTAMP,
            <if test="approvedLimit != null">approved_limit   = #{approvedLimit},</if>
            <if test="rejectionReason != null">rejection_reason = #{rejectionReason},</if>
            <if test="reviewedBy != null">reviewed_by      = #{reviewedBy},</if>
        </set>
        WHERE  application_id = #{applicationId}
    </update>

</mapper>
